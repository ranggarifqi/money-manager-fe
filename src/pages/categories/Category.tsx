import { usePageTitle } from "../../commons/hooks/usePageTitle";

const Category = () => {
  usePageTitle({
    title: "Categories",
    breadcrumb: [
      {
        label: "Categories",
      },
    ],
  });

  return <div>asdsad</div>;
};

export default Category;
