import { Menu, MenuItem } from "@blueprintjs/core";

const AvatarMenu = () => {
  return (
    <Menu>
      <MenuItem icon="log-out" onClick={undefined} text="Logout" />
    </Menu>
  );
};

export default AvatarMenu;
